# Research Exports

This directory is reserved for user-facing research exports such as external
CSV extracts, presentation datasets, and report bundles.

Exports are generated artifacts. They require explicit review before inclusion
in a source commit, and large payloads require a separately approved artifact
storage workflow.
