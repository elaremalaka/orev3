# Research Results

Generated and retained experiment outputs belong in this directory.

Current expected file:

- `timing_sweep_results.csv`

The checkpoint installer copies the existing repository-root `timing_sweep_results.csv` here when present.

Future files may include:

- `square_statistics.csv`
- `correlations.csv`
- `feature_importance.csv`
- walk-forward evaluation outputs
