# Contributing

Before adding a new strategy, identify the report that supports it.

Before adding a new feature, determine whether:

1. it already exists in a published dataset,
2. it was observable at decision time,
3. it requires a new dataset version,
4. its semantics can be tested.

Research changes should remain small enough that their question, result, and
decision are easy to review.
